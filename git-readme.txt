download windows - https://git-scm.com/download/win (link). 

Open git bash or right click the folder and select bin bash. 
#git init (add the path). 
#rm -rf .git (to stop tracking git) 
#ls -la
#git status
#git config --global user.name 'sb'
#git config --global user.email 'ssplsm@hotmail.com'
#git config --list
#git config --help  or git help config
#git add . (add all files) git add -A
#git add filename.txt
#git rm --cached filename.txt
#git reset filename.txt (to remove from staging area)
#git reset (to remove everything)
#git commit -m "comment"
# touch .gitignore  - Create directory to add ignore files. 
#git branch newbranch       create new branch 
#git checkout newbranch - to change the path to newbranch
#git checkout master   - go back to original branch
#git merge <path>

git log (view modifications)
git clone ../siby.git . (from local directory like remote repository)
git remote -v (list repository details)
git branch -a (list branches)
git diff (view changes locally)

git push origin master (pushing master branch to origin repository )
git pull


github -
sbm-git
ssplsm@hotmail.com
sbtest3586