download windows - https://git-scm.com/download/win (link). 

Open git bash or right click the folder and select bin bash. 
#git init (add the path). 
#git status
#git add . (add all files)
#git add "filename.txt"
#git commit -m "comment"
# touch .gitignore  - Create directory to add ignore files. 
#git branch newbranch       create new branch 
#git checkout newbranch - to change the path to newbranch
#git checkout master   - go back to original branch

